---
_content:
  - command: input ready, excited, vibe
  - goto: /tunnel/training
_include:
  logic: logic.yaml
  orphanage: orphanage.yaml
  tunnel: tunnel.yaml
_vars:
  # Important vars
  - energy: 0
    _locale: Energy
  - time: 0
  # Logic vars
  # Progress vars
  - tunnel_pass: training
  # Inventory vars
  - coin: 0
    _locale: Coin
  - rosalia: 0
    _locale: Rosalia
  # Misc. vars
  - is_expedition_day: True # It's initially an expedition day when the story starts
  - on_training_expedition: True # Start off in the middle of the expedition to the Tunnel
  - time_next_training: 0
_header:
  - pass:
